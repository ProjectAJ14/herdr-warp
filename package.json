{
  "name": "herdr-warp-release-tooling",
  "private": true,
  "description": "Release tooling for the herdr-warp plugin. Nothing is published to npm — the plugin is notify.py plus herdr-plugin.toml, installed by Herdr straight from this repo.",
  "scripts": {
    "release": "semantic-release",
    "release:dry": "semantic-release --dry-run --no-ci"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^24.2.3"
  }
}
